export class Product {
    constructor(public name: string, public description: string, public image: string, public price: number, public memorie: string, public platform: string, public display: string, public camera: string, public pack: string) { }
}